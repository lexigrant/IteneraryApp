<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <h1>Your Trips</h1>
    <nav>
        <a href="/itenerary/new"><button>Create New itenerary</button></a>
    </nav>
    <ul>
        <% for (let i = 0; i < Itenerary.length; i++) { %> 
            <li>
                <a href="/itenerary/<%=Itenerary[i]._id%>">
                <%=Itenerary[i].name%></a><br/><%=Itenerary[i].startDate?.toDateString()%> - <%=Itenerary[i].endDate?.toDateString()%><br/>Party Size: <%=Itenerary[i].party%>
                </a>
                <form action="/itenerary/<%=Itenerary[i]._id%>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE">
                </form>
                <nav>
                    <a href="/itenerary/<%= Itenerary[i]._id%>/edit"><button>Edit Itenerary</button></a> 
                </nav>
            </li> 
            <% } %>
    </ul>
</body>
</html>